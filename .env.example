# Thewavess AI Core - 環境變數配置範例
# 複製此文件為 .env 並填入您的實際設定值

# ===========================================
# 資料庫配置 (PostgreSQL)
# ===========================================
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_NAME=thewavess_ai_core
DB_SSLMODE=disable

# ===========================================
# AI 服務 API Keys (必填)
# ===========================================
# 支援 OpenAI 官方 API 或 Azure OpenAI Service
OPENAI_API_KEY=sk-your-openai-api-key-here
# 支援 X.AI Grok API 或 Azure AI Foundry
GROK_API_KEY=your-grok-api-key-here
# TTS 專用 API Key (可選，如果不設定則使用 OPENAI_API_KEY)
TTS_API_KEY=sk-your-tts-api-key-here

# ===========================================
# 伺服器配置
# ===========================================
PORT=8080
GIN_MODE=debug
ENVIRONMENT=development
LOG_LEVEL=debug

# Swagger API 主機配置 (動態設定 API 文檔的 host)
# 開發環境: localhost:8080
# 測試環境: thewavess-ai-core.clarence.ltd
API_HOST=localhost:8080

# ===========================================
# CORS 配置 (跨域請求設定)
# ===========================================
# 允許的來源 (預設為 * 全開，生產環境請設定具體域名)
CORS_ALLOWED_ORIGINS=*
# 允許的 HTTP 方法
CORS_ALLOWED_METHODS=GET,POST,PUT,PATCH,DELETE,HEAD,OPTIONS
# 允許的請求標頭
CORS_ALLOWED_HEADERS=Origin,Content-Length,Content-Type,Authorization,X-Requested-With,Accept,Accept-Encoding,Accept-Language,Connection,Host,User-Agent
# 暴露的回應標頭 (可選)
CORS_EXPOSED_HEADERS=

# ===========================================
# NSFW 內容設定 (永久開啟)
# ===========================================
NSFW_DETECTION_THRESHOLD=0.7
# 可選門檻（未設定時使用程式內預設值）
# 浪漫 L2 門檻（romantic 次數 ≥ 此值 → L2）
NSFW_ROMANTIC_L2_THRESHOLD=1
# 親密 L3 門檻（intimate 次數 ≥ 此值 → L3）
NSFW_INTIMATE_L3_THRESHOLD=1
# 親密 L4 門檻（intimate 次數 ≥ 此值 → L4；若不希望僅靠親密詞升 L4，設大一些）
NSFW_INTIMATE_L4_THRESHOLD=3
# 情趣/角色扮演 L4 門檻
NSFW_FETISH_L4_THRESHOLD=1
NSFW_ROLEPLAY_L4_THRESHOLD=1
# 明確/極端/違法 → L5 的門檻
NSFW_EXPLICIT_L5_THRESHOLD=1
NSFW_EXTREME_L5_THRESHOLD=1
NSFW_ILLEGAL_L5_THRESHOLD=1

# ===========================================
# 對話配置
# ===========================================
# OpenAI 設定 (處理 NSFW Level 1-4)
# OpenAI API URL (支援 Azure 或其他自定義端點)
# 標準 OpenAI: https://api.openai.com/v1
# Azure OpenAI: https://your-resource.openai.azure.com
OPENAI_API_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4.1
OPENAI_MAX_TOKENS=1500
OPENAI_TEMPERATURE=0.8

# Grok 設定 (處理 NSFW Level 5 極度明確內容)
# Grok API URL (支援 Azure 或其他自定義端點)
# 標準 X.AI: https://api.x.ai/v1
# Azure AI Foundry: https://your-resource.services.ai.azure.com
GROK_API_URL=https://api.x.ai/v1
GROK_MODEL=grok-4
GROK_MAX_TOKENS=2000
GROK_TEMPERATURE=0.9


# ===========================================
# 開發/測試配置
# ===========================================
TEST_MODE=false

# ===========================================
# JWT 配置 (未來使用)
# ===========================================
JWT_SECRET=your-super-secret-jwt-key-here
